# 战局/仓库背包与装备数据路径梳理（基于现有反射/补丁代码）

## 范围与说明
- 本文档基于当前仓库已有的反射/补丁点整理“已确认”的数据路径。
- 另外补充了基于 `Assembly-CSharp.dll` 反射确认的玩家/装备入口（来自 `Sources/Client/bin/Release/net471/Assembly-CSharp.dll`）。

## 已确认的数据入口（可直接复用）

### 1) 战局内交互物品（地面/容器交互）
- 入口链路：`ActionPanel -> GamePlayerOwner -> Player.InteractableObject -> LootItem -> LootItem.Item`。
- 该链路已用于地面物品名称着色，可复用为战局内“当前交互物品”的数据入口。
- 代码参考：`Sources/Client/Patches/LootItemLabelPatch.cs:171`、`Sources/Client/Patches/LootItemLabelPatch.cs:180`。

### 2) 仓库/物品栏 UI 入口
- 物品栏打开入口：`InventoryScreen.Show()`（可作为“仓库/物品栏已打开”的触发点）。
- UI 格子上的物品实例：`GridItemView.Item`，在鼠标进入时直接拿到 `Item`。
- 代码参考：`Sources/Client/Patches/InventoryScreenShowPatch.cs:22`、`Sources/Client/Patches/ItemHoverPatches.cs:24`。

### 3) 战局内玩家穿戴信息（装备槽）
- 战局玩家入口：`EFT.GameWorld.MainPlayer`（本地玩家），或 `EFT.GameWorld.AllAlivePlayersList`（所有存活玩家）。
- 玩家装备入口：`EFT.Player.Equipment`（类型 `EFT.InventoryLogic.InventoryEquipment`），等价于 `EFT.Player.Inventory.Equipment`。
- 槽位获取：`InventoryEquipment.GetSlot(EquipmentSlot slotName)` 或遍历 `InventoryEquipment.AllSlots`。
- 槽位物品：`Slot.ContainedItem` 即“玩家当前穿戴/携带在该槽位上的物品”。  
- 装备槽枚举：`EquipmentSlot`（见下方列表）。

**EquipmentSlot 枚举值（已确认）**：
`FirstPrimaryWeapon` / `SecondPrimaryWeapon` / `Holster` / `Scabbard` / `Backpack` / `SecuredContainer` / `TacticalVest` / `ArmorVest` / `Pockets` / `Eyewear` / `FaceCover` / `Headwear` / `Earpiece` / `Dogtag` / `ArmBand`

**最小示例（伪代码）**：
```
var gw = Singleton<GameWorld>.Instance; // 常用入口
foreach (var p in gw.AllAlivePlayersList)
{
    var equip = p.Equipment;
    var headwear = equip.GetSlot(EquipmentSlot.Headwear)?.ContainedItem;
    var armor = equip.GetSlot(EquipmentSlot.ArmorVest)?.ContainedItem;
}
```

### 4) 物品归属（是否属于装备树）
- 使用 `Item.Parent.GetOwner().RootItem is InventoryEquipment` 判断当前物品是否属于装备树。
- 代码参考：`Sources/Client/Patches/SearchPatch.cs:158`。

### 5) 背包/容器数据（内容物）
- 容器判定：优先读取 `IsContainer` 属性；备用方式是检测 `Grids` 是否存在/非空。
- 容器内容：`Grids -> Items`，每个 `Item` 仍可继续递归。
- 兼容属性名变化：`Grids / Grid / Containers / Container`（反射尝试多个名字）。
- 代码参考：
  - `Sources/Client/Patches/TestTooltipPatch.cs:1818`
  - `Sources/Client/Patches/TestTooltipPatch.cs:1774`
  - `Sources/Client/Patches/TestTooltipPatch.cs:2056`

### 6) 装备槽/配件槽（武器与配件树）
- 配件槽：`Mod.Slots`，每个 slot 内通过 `slot.ContainedItem` 取子配件。
- 适用于武器与配件树的递归遍历。
- 代码参考：`Sources/Client/Services/PriceCalculator.cs:159`。

### 7) 护甲/插板槽（护甲内部槽位）
- 护甲内部槽位：`AllSlots -> slot.ContainedItem`（含插板/内衬）。
- 代码参考：`Sources/Client/Utils/ArmorHelper.cs:101`、`Sources/Client/Utils/ArmorHelper.cs:118`。

### 8) 快速枚举第一层物品
- `Item.GetFirstLevelItems()` 可直接拿到容器第一层物品。
- 代码参考：`Sources/Client/Patches/SearchPatch.cs:208`。

## 数据结构示意（从 Item 出发）
- 装备/配件树：`Weapon.Mods` 或 `Mod.Slots -> slot.ContainedItem -> Mod (递归)`。
- 背包/容器树：`Item (IsContainer/Grids) -> Grid.Items -> Item (递归)`。
- 护甲插板：`Item.AllSlots -> slot.ContainedItem -> ArmorPlateItemClass`。

## 需要反编译确认/补充
- 远端玩家（AI/其他PMC）的装备/背包是否在客户端有完整可读结构与更新时机。
- 具体 `GameWorld` 实例获取路径（如 `Singleton<GameWorld>.Instance` 的可用性）在当前版本需实测确认。

## 反编译定位建议（Assembly-CSharp.dll）
- 关键类型关键词：`GameWorld`, `Player`, `Profile`, `Inventory`, `InventoryController`, `ItemOwner`, `InventoryEquipment`, `EquipmentSlot`, `Stash`。
- 重点关注：玩家列表来源、Profile 到 Inventory 的字段链路、装备槽集合/枚举定义。

## 结算文本显示工具
- 公共工具类：`Sources/Client/Utils/RaidSummaryMetrics.cs`。
- 全局变量：`BroughtValue`（带入价值）、`LossValue`（损耗价值）、`LootValue`（收获价值）、`SettlementValue`（结算价值）。
- 结算显示：`BuildSettlementText()` 会根据 `SettlementValue` 正负返回带颜色的 TMP 富文本字符串。
- 格式约定：`FormatRuble(long)` 固定输出卢布单位与千分位（示例：`₽100,000,000` / `-₽100,000`）。
- 使用位置：`Sources/Client/Patches/SessionResultExitStatusPatch.cs` 在结算页写入“本局收获”文本。

## 服务端 XML 文档位置（SPTarkov.Server.Core）
- 文件位置：`SPT/SPTarkov.Server.Core.xml`（位于仓库上级的 `SPT` 目录）。
- 作用：服务端程序集 `SPTarkov.Server.Core` 的 XML 文档注释汇总，包含接口/事件描述与关键类说明。

## 修理/耐久相关条目（来自 SPTarkov.Server.Core.xml）
- 回调层：`SPTarkov.Server.Core.Callbacks.RepairCallbacks.TraderRepair` / `RepairCallbacks.Repair`（商人修理/修理包）。
- 控制器层：`SPTarkov.Server.Core.Controllers.RepairController.TraderRepair` / `RepairController.RepairWithKit`。
- 服务层：`SPTarkov.Server.Core.Services.RepairService.RepairItemByTrader` / `RepairService.RepairItemByKit` / `RepairService.PayForRepair`。
- 耐久更新与退化：`SPTarkov.Server.Core.Helpers.RepairHelper.UpdateItemDurability`、`RepairHelper.GetRandomisedArmorRepairDegradationValue`、`RepairHelper.GetRandomisedWeaponRepairDegradationValue`。
- 随机耐久生成：`RagfairOfferGenerator.RandomiseWeaponDurability`、`RandomiseArmorDurabilityValues`、`FenceService.RandomiseArmorModDurability`。

## 服务端修理逻辑（反编译摘要）
> 反编译文件目录：`.tools/decompile_server/`

### 1) 商人修理价格公式（RepairService.RepairItemByTrader）
- 文件：`.tools/decompile_server/SPTarkov.Server.Core.Services.RepairService.decompiled.cs`
- 关键字段：
  - `TemplateItem.Properties.RepairCost`：物品单点维修成本（基准值）。
  - `RepairItem.Count`：本次修理的耐久点数。
  - `TraderRepair.RepairPriceCoefficient`：商人维修系数（百分比）。
  - `RepairConfig.PriceMultiplier`：全局价格倍率。
- 公式（代码原样逻辑）：
```
repairPriceCoeff = (RepairPriceCoefficient <= 0) ? 1.0 : (RepairPriceCoefficient / 100 + 1.0)
repairCost = round(RepairCost * RepairItem.Count * repairPriceCoeff * RepairConfig.PriceMultiplier)
```

### 2) 耐久更新与最大耐久衰减（RepairHelper.UpdateItemDurability）
- 文件：`.tools/decompile_server/SPTarkov.Server.Core.Helpers.RepairHelper.decompiled.cs`
- 核心步骤：
  1. `Durability` 与 `MaxDurability` 同步增加 `amountToRepair`（并裁剪不超过原上限）。
  2. 若开启 `applyMaxDurabilityDegradation`：
     - 护甲：使用 `ArmorMaterial` 对应的 `MinRepairDegradation/MaxRepairDegradation` 随机生成衰减值。
     - 武器：使用 `TemplateItemProperties.MinRepairDegradation/MaxRepairDegradation` 随机生成衰减值。
     - 最终衰减：`random(min, max) * MaxDurability * traderQualityMultiplier`（保留 2 位小数）。

### 3) 修理请求与返回结构（类结构）
- `RepairItem`（文件：`.tools/decompile_server/SPTarkov.Server.Core.Models.Eft.Repair.RepairItem.decompiled.cs`）
  - 字段：`_id`（物品 Id）、`count`（修理点数）。
- `TraderRepairActionDataRequest`（文件：`.tools/decompile_server/SPTarkov.Server.Core.Models.Eft.Repair.TraderRepairActionDataRequest.decompiled.cs`）
  - 字段：`tid`（商人 Id）、`repairItems`（RepairItem 列表）。
- `RepairActionDataRequest`（文件：`.tools/decompile_server/SPTarkov.Server.Core.Models.Eft.Repair.RepairActionDataRequest.decompiled.cs`）
  - 字段：`target`（待修物品 Id）、`repairKitsInfo`（修理包信息列表）。
- `RepairDetails`（文件：`.tools/decompile_server/SPTarkov.Server.Core.Services.RepairDetails.decompiled.cs`）
  - 字段：`repairCost`、`repairPoints`、`repairedItem`、`repairedItemIsArmor`、`repairAmount`、`repairedByKit`。

